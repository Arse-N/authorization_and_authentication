package com.stellarlabs.authentication_and_authorization_service.model;

/** Those tree roles is for our users to control their businesses */
public enum UserRole {
    ADMIN,
    USER,
    MANAGER
}
